PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=notes_app
# Set to true when connecting to a managed MySQL provider that requires TLS
# (e.g. TiDB Cloud Serverless). Not needed for local MySQL.
DB_SSL=false
JWT_SECRET=
EMAIL_USER=
EMAIL_PASS=
BYPASS_EMAIL=false